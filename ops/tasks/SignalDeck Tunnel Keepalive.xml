<Task version="1.3" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Description>Restarts SignalDeck Tunnel if it has died, but ONLY inside the weekday 06:20-13:10 PT collection window. A naive keepalive would hold the tunnel open at 2am and silently convert an on-demand service into permanent internet exposure, which is the one property the on-demand design exists to protect. Outside the window it exits 0 having done nothing. It does NOT detect alerts lost while the tunnel was down - TradingView times out in about 3s with no meaningful retry, so a missed alert is discarded, not queued.</Description>
    <URI>\SignalDeck Tunnel Keepalive</URI>
  </RegistrationInfo>
  <Principals>
    <Principal id="Author">
      <UserId>{{USER}}</UserId>
      <LogonType>S4U</LogonType>
    </Principal>
  </Principals>
  <Settings>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <ExecutionTimeLimit>PT10M</ExecutionTimeLimit>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <StartWhenAvailable>true</StartWhenAvailable>
    <IdleSettings>
      <StopOnIdleEnd>true</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
    <UseUnifiedSchedulingEngine>true</UseUnifiedSchedulingEngine>
  </Settings>
  <Triggers>
    <LogonTrigger>
      <UserId>{{USER}}</UserId>
    </LogonTrigger>
    <TimeTrigger>
      <StartBoundary>2026-09-19T00:00:00-07:00</StartBoundary>
      <Repetition>
        <Interval>PT10M</Interval>
        <StopAtDurationEnd>true</StopAtDurationEnd>
      </Repetition>
    </TimeTrigger>
  </Triggers>
  <Actions Context="Author">
    <Exec>
      <Command>powershell.exe</Command>
      <Arguments>-NoProfile -ExecutionPolicy Bypass -File "{{REPO}}\ops\tunnel-guard.ps1"</Arguments>
      <WorkingDirectory>{{REPO}}</WorkingDirectory>
    </Exec>
  </Actions>
</Task>