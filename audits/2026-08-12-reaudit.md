# SignalDeck re-audit — 2026-08-12

Machine-checked companion to `audits/SIGNALDECKFIX_2026-08-12.md`, which carries
the full narrative. This file exists so THIS session's findings are tracked by
`tools/audit_register.py` like every other audit's.

That is not bookkeeping for its own sake. The largest single item this session
closed was eleven July findings that had been **fixed but never marked**, which
left the register failing for weeks. A narrative-only audit repeats exactly that
failure, one audit later. Every row below therefore carries a status inside the
closed vocabulary `{fixed, refuted, accepted-risk, open}` and cites evidence.

Branch: `hmm-regime-and-pbo`.

---

| id | area | severity | impact | evidence (measured) | status |
|---|---|---|---|---|---|
| **A1** | deployment | CRITICAL | None of the 2026-08-11 audit's daemon repairs were RUNNING. The live daemon executed a binary built 2026-08-09 15:16 (`f2119e8`), 19 commits behind, while every repaired file was modified after that build. The prior audit verified with unit tests against source and never checked what was deployed. | `worker_runs.revision` carried `f2119e8` on all 2,807 runs since that build; `git rev-list --count f2119e8..HEAD` = 19. Deployed and re-verified: provenance now answers `OK - binary is HEAD`, and `forecast-monitor` stopped emitting the false RAW MODEL COLLAPSE. | **fixed** |
| **A2** | ops | CRITICAL | The stale-binary preflight had NEVER executed. `ops/daemon-guard.ps1` guarded it with a `Test-Path` that fails open, and the script it guarded did not exist — only an uninstalled draft. The 2026-08-11 "fix" made the absence print a warning instead of restoring the file. | Installed to `ops/`; run live it reported the 19-commit gap it was written to catch, then `OK - binary is HEAD` after deploy. | **fixed** |
| **A3** | quant | CRITICAL | Symbol pages published 17,099 observations at 47.7% while `/accuracy` published 2,399 at 41.5% for the same predictor — the app overstating its own evidence 7.1x against its own scoreboard, because no Go surface applied the grader's epoch, settlement or stale-feed filters. | `internal/store/gradeablepop.go` is now the single Go definition; the SQL it emits reproduces the grader population exactly on the live corpus (1d n=2399 acc=0.41517; 1w n=3186 acc=0.37916). | **fixed** |
| **A4** | quant | HIGH | The grader folded on the calendar day, which `settleday.go` had already measured as a 1.41x overstatement of independent N. `settle_ts` was 100% backfilled and unused. | Go and Python now both fold on `settle_day`, verified identical on the live corpus (1d n=2380 acc=0.429412; 1w n=2256 acc=0.381397). Landed with prereg chain AMENDMENT seq 61. | **fixed** |
| **A5** | quant | HIGH | `MIN_DISTINCT_BLOCKS` could be satisfied by degenerate days: 1d had 14 "days" of which 4 carried 1, 2, 4 and 6 observations — under 1.5% of the rows supplying a third of the count that authorised publishing an interval. | Admission now counts CREDIBLE blocks at a fraction of the sample's own median day. Two absolute predicates were refuted with data first (a fixed row floor misfires on a 3-symbol universe; call-day coverage is uncorrelated with graded thinness — a day with 98% coverage still yields 1 graded row). `directional-ensemble (1w)` moved FAILED → INSUFFICIENT DAYS (7/10 credible of 11). | **fixed** |
| **A6** | honesty | HIGH | The daemon ships `evidenceCaveat` ("BACKTEST CLAIM, not a live measurement") on every forecast and the web dropped it at the TYPE layer, so it had ZERO render sites while a tile read "MEASURED ACCURACY". | `grep evidenceCaveat web/src` returned 0 hits. Added to both interfaces and the hand-written cast that also omitted it; tile relabelled and the caveat rendered verbatim. tsc and eslint clean. | **fixed** |
| **A7** | honesty | HIGH | Four surfaces called an ungraded backtest constant "measured accuracy" — including the markdown EXPORT, where the figure leaves the app with only its column header as context — while `resolved_at` is NULL on all 37,857 structural rows. Two hero tiles also summed the not-measured sentinel (0) into their means, so a row rendered "—" was counted as 0%. | All four relabelled BACKTESTED; both tiles now average only measured rows and print the contributing count. | **fixed** |
| **A8** | honesty | HIGH | Hard-coded "12,931 symbol-days / 48.08% / 54.50%" on every symbol page, undated and present-tense beside a live card. | Traced: no source in the repo produces those numbers, and the registry reports `acc: null`, `n: null`, baseline 55.2%. Replaced with the qualitative verdict and a link to `/accuracy`, per the pattern `TodaysRead.tsx` already established. | **fixed** |
| **A9** | honesty | MEDIUM | Three `?? 0` sites published invented zeros: "HIT RATE 0.0%", "P(up) 0.0%" (which reads as near-certainty of a DOWN move, an inversion not a blank) and a missing paper return as "0.00%" in green. | `StatTile`'s own comment already called a `?? 0` reaching it "a bug"; all three now render the no-data state. | **fixed** |
| **A10** | daemon | HIGH | Six workers reported success while doing nothing — `confluence`/`smartmoney` returned nil on total write failure, `model-health` and `feature-health` counted work before persisting it, retention named the wrong cause and pointed at a dq record nobody wrote, and the schema-contract publish was swallowed so health could report ALL CLEAR while workers were de-registered. | Each now degrades or logs; `"degraded is not failing"` is pinned by `health_test.go:186` so none of it can 503 readiness. 122 Go packages pass. | **fixed** |
| **A11** | daemon | HIGH | Nothing self-probed the API listener. `api.Serve` is not a Worker, so it had no `worker_runs` row and `FailingWorkers`/`StaleWorkers` were structurally blind: with the listener dead, health.Check still said "healthy: N workers checked" and the cache-warmer stayed green because it calls its build funcs in-process. | New `api-probe` worker over real TCP; live: `api listener answering on 127.0.0.1:8322 (HTTP 200)`. 5 tests pin both directions. | **fixed** |
| **A12** | daemon | HIGH | `ShutdownGrace` is 75s but `minRunTimeout` is 15 min, so an operator stop during a long worker run hit `forceExit(1)` — which main.go defines as "internal fault, supervisor restarts" — and Task Scheduler restarted the daemon the operator had just stopped. | `Runner.OperatorStop` keys off the parent signal context. Both exit codes pinned in both directions. | **fixed** |
| **A13** | ops | HIGH | The plist→Task translator read NEITHER `StandardOutPath` nor `StandardErrorPath`, so redirection was dropped wholesale on the Windows move: Cleanup reported `0x0` daily for 14 days with its log last written 07-29. | Proved both shapes — the old argument form creates no log; `bash -lc "… >> log 2>&1"` creates it with content — then end-to-end via a scheduled run. | **fixed** |
| **A14** | ops | HIGH | `install-windows-tasks.ps1 -Install`, this file's own documented recovery path, would have repointed the live Daemon task through bash and broken graceful stop (`schtasks /End` does not cascade to children). | Refused explicitly, with the guard placed BEFORE trigger translation — its first placement sat after the "no schedule" skip, where it would never have run. Dry run shows the refusal. | **fixed** |
| **A15** | ops | HIGH | Both fleet health gates had no scheduler entry and could not have one: the translator accepted only `.sh` and both are PowerShell. `fix-task-principals.ps1` told the operator to "watch for recurrence" as a manual instruction. | `.ps1` is now first-class; both registered and verified (daily 09:05 / 09:20). | **fixed** |
| **A16** | ops | HIGH | Running `-Install` converted 11 batch tasks from S4U to INTERACTIVE, re-exposing them to console-control kills (`0xC000013A`) — the exact failure `fix-task-principals.ps1` exists to end. `Register-ScheduledTask` with `-RunLevel` and no `-Principal` defaults to Interactive. CAUSED BY THIS SESSION. | Measured: fleet went Interactive=0/S4U=14 → Interactive=11/S4U=5, caught by the health gate on its first scheduled run. The principal is now set explicitly so it cannot recur. The four long-running services are unaffected. REMEDY VERIFIED: `ops/fix-task-principals.ps1` filters on `$Match = 'SignalDeck'`, which matches all 11 by name INCLUDING the two health-check tasks created today, so the handover covers everything. Four distinct mechanisms were tried unelevated and all refused: `Set-ScheduledTask -Principal`, `Register-ScheduledTask -Force -Principal`, `schtasks /Create /XML /F`, and a hand-edited exported definition — Access is denied on each. Changing a task to S4U needs an admin right; this is an OS boundary. RESOLVED 2026-08-13: Nicholas ran `ops/fix-task-principals.ps1` elevated. Verified by counting principals rather than trusting the run — all **16 of 16** SignalDeck tasks now report `LogonType=S4U` and **Interactive is zero**, so the regression is not merely reverted for the 11 I broke but closed across the whole fleet. `ops/check-task-health.ps1` confirms the same and no longer emits its "still Interactive - reachable by console control events" warning. | **fixed** |
| **A17** | ops | MEDIUM | Three KILL paths in the eighty loop hit `continue` before `Commit-Draft`, so a died cycle left its script modified forever — blocking every deploy, which is what `h0626.py` did throughout this session. | All three now commit. A fourth insertion was made and REMOVED: that path runs before `$script` is assigned and would have committed the previous cycle's file. | **fixed** |
| **A18** | ops | MEDIUM | The self-improve loop's accept gate had drifted from `GateSpecs`: `py-tests` re-briefed the stale five-module list that GateSpecs' own comment says it replaced with discovery, so a failure in any other file could be "verified fixed" by five unrelated modules passing. | Both now look the command up from `GateSpecs` and cannot drift again. Measured: the stale re-brief named 5 modules while `tools/` holds 16 discoverable `test_*.py`, so a failure in any of the other 11 could be "verified" by 5 unrelated ones passing. `ops/test-selfimprove-loop.ps1` passes 20 of 21 (the 1 failure is pre-existing and unrelated, verified identical before and after). | **fixed** |
| **A19** | ops | MEDIUM | `signaldeck-cleanup.sh` "reclaims disk" into `~/.Trash`, a macOS concept; under Git Bash nothing ever empties it, so the task moves bytes and frees nothing — and the pile sits outside `assert_budget`'s field of view. | Measured 10,044 MB on the SAME volume as the data. Nothing is deleted (retiring a backup is not destroying it); the size and volume are now printed every run. | **fixed** |
| **A20** | honesty | MEDIUM | `research_liveness` printed `OK — every narrated grid search has a matching judgment ledger` directly beneath four ACKNOWLEDGED UNVERIFIABLE entries saying the opposite, one of them "the judgments were never persisted". `partition_violations`' own docstring already promised they were "counted in the exit summary". They were not. | Summary now names the acknowledged count. Refusal policy and exit code unchanged; siblings checked and found honest. Measured: 4 acknowledged-unverifiable claims were printed and then contradicted by the summary line; one reads "run row claims judged=48 but research_loop_judgments holds 0 rows for the day". Exit code stays 0 and `test_research_liveness.py` passes. | **fixed** |
| **A21** | ops | HIGH | `deployment_drift` REFUSED PUBLICATION on a false diagnosis. `newest_boot_ts` infers a boot from a quiet stretch, so deploys closer together than that leave several binaries inside the window; it reported "the deployed binary is not stamping the rows it writes" when the binary was stamping perfectly. | Measured: boot read 18:42 while the running revision began at 21:10, ratio 4.7%. A revision CHANGE is an exact boot signal; `newest_boot_ts` now takes the later of the two. After: worker_runs 125/125, prediction_ledger 8/8, exit 0. | **fixed** |
| **A22** | honesty | MEDIUM | `lab/pairs` said its numbers were "NOT restatements of the ledgered 73.1%, which measured SPY-correlation tiering". The sentence exists to separate two quantities and misdescribed the one it separates FROM: 0.731 is trend21's registered claim about 200-day-MA side persistence, and it is backtested, not "ledgered". | Verified against `data/accuracy_registry.json` and the prereg spec. Description corrected; the figure itself is real and stays. | **fixed** |
| **A23** | infrastructure | HIGH | "Offsite" backup is on the same physical volume as the data, so it dies with the disk. | Verified, not assumed: ONE physical disk (Kingston 1.9 TB), only `C:` plus a 1.3 GB Recovery partition, no mapped network drives, OneDrive present but not signed in (all accounts empty). The guard already refuses to claim otherwise and withholds `backup_last_offsite`. Needs an external volume — hardware, not code. | **accepted-risk** |
| **A24** | ops | MEDIUM | The web UI serves a stale bundle: Q1/A22's fixes are committed and `next build` succeeded, but the running process cannot be restarted from an unelevated session. | All three routes exhausted and proven: `Stop-ScheduledTask` does not cascade, `schtasks /End` returns SUCCESS while the socket stays held (its parent had already exited, leaving a session-0 orphan), and `taskkill /F` / `Stop-Process` return `Access is denied`. The related gate defect IS fixed — `check-task-health.ps1` now asks the ports, pinned both directions, and runs daily. REMEDY VERIFIED: the web bundle was REBUILT at 2026-08-12T21:41:47 so a restart serves the current code — the earlier build predated the A22 pairs fix, and handing over a remedy that would have served stale code was itself the trap. Confirmed the bundle now contains all three web fixes ("registered 73.1% claim", "BACKTEST ACCURACY", "Backtested accuracy in this conviction band"), and the task's action is intact (`next start -p 8323` with both logs redirected), so `Start-ScheduledTask 'SignalDeck Web'` after the kill is sufficient. REMEDY NOW SCRIPTED as `ops/restart-web.ps1`, which removes a hazard the earlier handoff carried: it said "kill PID 29984", and a PID written into a runbook goes stale the moment anything restarts — a stale PID in a script that runs elevated is a way to kill the wrong process. The port is the stable identifier, so the script resolves the holder of 8323 at RUN TIME, and refuses unless that holder is BOTH on the port AND named `node`, because this machine also runs the daemon and other sessions' work. It then waits for a real HTTP response rather than trusting the start call — a started task is not a serving web app, and that exact gap is what let the stale bundle sit unnoticed. Verified functionally, not just parsed: run unelevated it exits 2 with the refusal and the port holder is unchanged (29984 before and after), and it parses clean with a parser that actually ran (0 errors) and is ASCII-only for PS 5.1. Still **open** because applying it needs the elevation this session could not obtain. RESOLVED 2026-08-13: Nicholas ran `ops/restart-web.ps1` elevated. The orphan is gone — port 8323 is now held by PID 33176, not the 29984 that had survived every unelevated attempt — and the task shows `Running` under S4U. Proving it serves CURRENT code took one correction worth recording: hunting the three fix strings in the served pages returned NOT FOUND twice, which looked like the stale bundle persisting, but the fault was my own bookkeeping — `BACKTEST ACCURACY` lives in `TodaysRead.tsx` and the signals report page, NOT `/lab/track-record` as the earlier row implied, the third string does not exist in source as recorded, and Next serves chunks lazily per route so absence from one page's HTML proves nothing. The decisive check is byte identity: `/_next/static/chunks/3z_0n2omxe416.js` served by the running server and the same file in `web/.next` hash **identically** (`e78cd952541e91837b91e5a98261a0b0`), and the A22 pairs fix string `registered 73.1% claim` IS present in a served chunk. So the running server is serving the on-disk build, which is the 21:41:47 rebuild carrying the web fixes. | **fixed** |
| **A25** | ops | MEDIUM | The stale-binary preflight raised its full STALE BINARY alarm for a delta the binary provably cannot contain. After a run of documentation commits it reported "6 commits behind" while the Go tree was byte-identical to the deployed revision, so every run said STALE and none of them meant it. A check that cries wolf is one operators learn to route around — and this check guards the exact failure that would then hide, a genuinely stale collector, which is finding A1 of this same audit. | It now separates commits that can reach the binary from ones that cannot, and only downgrades the latter. Proven both directions on real ranges: the current docs-only delta reports `OK - binary is current in substance (6 commit(s) ahead, none touching the daemon)`, while d09162c..HEAD (11 code files), 82b2a2a..HEAD (10) and 1f47277..HEAD (7) all still report STALE. Strictly narrower, never wider — anything touching `daemon/`, `*.go`, `go.mod` or `go.sum` is stale exactly as before, and if git cannot answer the count stays at the full `behind` so doubt falls toward reporting staleness. | **fixed** |
| **A26** | security / process | LOW | SELF-INFLICTED: while closing 2026-07-26:A13 I pasted the finding's own example token into its evidence cell, so a TRACKED audit file began matching `VENDOR_PATTERN` and `ops/pre-publish-scan.sh` started reporting a secret in tracked files. The token is synthetic (a repeating-character placeholder, never a credential) and there is no exposure, but a secret scan that fires on the audit describing secret scanning is noise in the one gate that must stay readable. | Working tree is clean again: the literal is replaced by a description and the TRACKED-files section of the scan reports 0 hits. It remains in git HISTORY from commit 7799799, where the scan still finds it — NOT removed, because that needs a history rewrite on a branch three other worktrees share, and NOT excluded, because this scan's own instruction is to fix the finding and never relax the gate. The history section was already permanently red on a decoy Alpaca key in `round2-drafts/patches/`; this adds a second entry of the same kind. Lesson recorded rather than hidden: never paste a matchable token into a tracked file, not even as evidence that the matcher works. | **accepted-risk** |
| **A27** | process / coverage | HIGH | The register cannot see `audits/2026-08-03-adversarial-reaudit.md`, so five findings went untracked for nine days. TWO independent reasons, either alone sufficient: `FILE_DATE_RE = (\d{4}-\d{2}-\d{2})-reaudit\.md$` does not match the `-adversarial-` infix, AND the file states its findings as `## F-N (SEVERITY) — …` headings with no pipe-delimited table to parse. This is the same defect class as A20/A25 and as the eleven July findings this session closed: a check that looks like it covers something and silently does not. Untracked meant unaudited — nobody re-checked them, and F-2 below is still live because of it. | Measured: of 9 files in `audits/`, only 3 are TRACKED; the other 6 (including this one) are skipped. Re-verified all five 2026-08-03 findings by hand against live code today — F-1 fixed (`page.tsx:276` handles `REFUSED` and cites the defect in-comment), F-3 fixed (verdicts are data-driven off `r.verdict` with an explicit `WITHHELD` fallback), F-5 fixed (grader ran 21:33 today; registry `generated=2026-08-12T21:33:12`), F-4 REFUTED — its acute claim was "WAL checkpoint starvation; the likely cause of the standing grading refusal", and grading now runs while the WAL sits FLAT at 64.0MB (max 65.5) across all 20 recorded governor passes. TRUNCATE is denied on every one of those 20, but that is the case the ladder is built for: the lower rungs reclaim 900–2000 frames a pass and hold the WAL at 1.3% of a 4.9GB database, so `status=ok` is honest reporting of a pass that did real work. The 128MB alert is correctly calibrated, not blind — it fired 136 times between 07-17 and 08-11 when the WAL genuinely ran away (peak 141.5MB) and 0 times in the last 24h. I nearly filed this as a live defect on the strength of "0/20 truncations" before checking alert recency; the frame-stall detector genuinely cannot fire here (the frame index moves every pass), but nothing is being missed — and F-2 STILL OPEN, see below. Four of five closed only by luck, never by a gate. | Both causes fixed. `FILE_DATE_RE` now allows a descriptive infix, `ROW_RE` accepts the `F-N` ids this audit's own prose cites (renaming them to suit the parser would break every cross-reference), and — the part that actually closes the hole — a file that DECLARES findings but yields no parsable rows is now REPORTED as `UNTRACKED AUDIT`, never skipped. The discriminator was checked against the real tree before it was written: of 9 files, 3 declare a table, 1 declares `## F-N (SEVERITY)` headings, and 5 are genuine narrative or research notes, so it fires exactly once with zero false positives. Verified both directions on the real directory: it FAILED naming `2026-08-03-adversarial-reaudit.md: it states 5 finding(s) as headings (F-1..F-5) with no status table`, and after that audit was given the status table it never had, the register reads **58 findings across 4 audits, exit 0**. One existing test asserted the defect itself — a full findings table with a bogus status, ignored purely for its filename — so its fixture was corrected to a realistic research note rather than deleted, and 4 tests were added for the new behaviour (unmatched name reported, infix parsed, heading-style reported, `F-N` ids parsed). 20 register tests, 330 Python tests green. | **fixed** |
| **A28** | quant / api | MEDIUM | 2026-08-03:F-2 is UNFIXED after nine days: `/api/recommendation` serves a stale price under a current timestamp. `recommendation.Inputs` still has `HasPrice bool` + `Price float64` and **no age field** (`recommendation.go:52`), so the engine cannot gate on price age even in principle, while every other leg is age- or availability-gated (`PredAgeSec` is threaded into conviction and discounted past `convStaleAgeSec`). Build()'s own comment already promises the missing behaviour — expected return is "only defensible when BOTH a heuristic fair value and a **live** price exist" — but the code only ever checks that a price EXISTS. | Exposure MEASURED, not assumed, and it is narrow: across 329 active non-delisted symbols the newest 1d bar is median 1.0 days old, p90 1.0, and only 3 exceed 2 days — but EA is 9 days stale and MVO 20 days, each served with a current timestamp and no way for a consumer to tell, and `ExpectedReturnPct` is computed off that close. A runnable check is written and pinned RED against current code (`audits/F-2-price-staleness_test.go.pending`, 5 cases incl. a long-weekend false-positive guard and a zero-value back-compat guard); dropping it into `daemon/internal/recommendation/` reproduces the defect as a compile error on the absent field. Fixed in commit 58a11e0 after Nicholas authorised Claude for this one task — both the Cloudflare and Google lanes returned HTTP 429 on real completions, so the standing stop-and-ask condition fired first and the work waited for his answer rather than being absorbed silently. `Inputs` gains `PriceAgeSec`, gated at `priceStaleAgeSec` (5 days, deliberately clear of a long weekend); `risks()` discloses the age; `CurrentPrice` is unchanged because the last known price is still worth showing, just not undated. BOTH API call sites populate the age from the bar timestamp — that wiring is the half that matters, since a gate which compiles and passes its unit test while nothing ever sets the field is the same "reports success while doing nothing" shape as A20/A25. Proven end-to-end against the REAL database rather than a fixture: exactly EA (9.1d) and MVO (20.1d) withheld with the disclosure line, 327 symbols kept, zero false positives — and the 9.1/20.1 figures matching the independent SQL measurement is what confirms the bar timestamp is in the unit the arithmetic assumes, the one semantic risk `go build` cannot catch. `TestExpectedReturnRefusedOnStalePrice`, `TestExpectedReturnSurvivesLongWeekend`, `TestUnsuppliedPriceAgeIsNotStale`; RED before, 122 Go packages green after. | **fixed** |
| **A29** | quant | HIGH | `forecast-monitor` is the only worker returning `error` on the current binary, and it is RIGHT to: forecast coverage collapsed on 2026-08-07 and has never recovered. Separately, the considered universe jumped from 329 to 2947 symbols on 2026-08-11. AMENDED after diagnosis — that jump is NOT a defect and my first write-up calling it undocumented was wrong: a survivorship backfill recorded true delisting dates for 1870 symbols and flipped them to `active=0`, and until it ran they legitimately looked live to a predictor that already filters on `active=1`. The predictor is behaving correctly and has excluded them since. What survives is narrower and still real: on the affected days the coverage RATIO is computed over a denominator containing 1870 symbols since reclassified, so 08-11 and 08-13 are not comparable to earlier days, and the headline UNDERSTATES the collapse — 187 of the 286 forecasts on 08-13 went to symbols now known to be delisted, so true live-universe coverage is 19/329 = **0.058**, not the reported 0.097. DIAGNOSED — and the conclusion is that coverage is NOT a defect to fix. Traced the withholding to its cause: `RequireMeasuredLegs` is now default-ON, so a leg is admitted only on measured out-of-sample ranking (`RankEdge`, graded AUC from `model_forecasts`, which is live — pressure and expectancy were both regraded at 23:36 today). Measured the grades over the newest 50 rows per (model, horizon), and nearly every leg ranks BACKWARDS against the 0.5 no-skill line: pressure 1w **0.261**, pressure 1d 0.379, alphax 1d 0.400, expectancy 1d 0.446, gbm 1d 0.450, alphax 1w 0.491. Only meanrev clears it (1d 0.543, 1w 0.578) plus expectancy 1w at 0.502. With no leg admitted, `AdmittedProbability` returns ok=false and the caller emits "no forecast" instead of a 0.5 that would read on the wire exactly like a real coin-flip call. So the collapse is the integrity machinery doing precisely its job — refusing to publish forecasts from legs with no demonstrated edge. Raising coverage means either improving the models (research) or lowering the gate (dishonest); it is emphatically not a bug fix, which is why it stays open for judgment rather than being "repaired". Recorded as an observation and NOT a recommendation, because this repo's own history is full of exactly this trap: pressure 1w at 0.261 is far enough below 0.5 to be informative inverted, but n_eval is 1016 on a single window and treating that as a signal is the overfitting the PBO/SPA work exists to catch. TRAP WORTH KNOWING, because it nearly produced a false finding here: `tools/leg_audit.py` reports **expectancy HELPED, AUC 0.5199 over 39 days, admitted 0 times**, which reads like a leg with proven edge being wrongly excluded. It is not. `rankGate` (predict.go:458) admits on the day-clustered WITHIN-DAY cross-sectional AUC via `fleetVetoes`, deliberately, because that is how the leg is actually used — ranking symbols within one day — whereas leg_audit's figure pools across days. The code comment records that the two measures disagreed enough to flip a verdict on the live 1d pressure record (0.3614 vetoed outright versus 0.4636 straddling chance), and leg_audit's own footer says its verdicts "are read off the day-clustered interval, never the point estimate" and are "not a published verdict". So a HELPED row beside a 94% withholding rate is two statistics answering different questions, not a contradiction. Anyone reconciling coverage against leg_audit must compare within-day cross-sectional AUC, not the pooled column. Note also that leg_audit's `admit` column spans 39 days and therefore MIXES the pre- and post-tightening regimes, so it cannot be read as current admission behaviour — measure today's rows for that. DECISION SUPPORT, measured on TODAY's rows only to avoid exactly that mixing: `n_used` never exceeds **1**. Of 3,401 deduped 1d rows in the last 24h, 2,661 are withheld (`n_used=0`) and 740 carry exactly ONE admitted leg; not a single row blends two. So the platform is not currently running an ensemble at all — it is one leg behind a gate, which also refutes any reading of the coverage number as "the blend is thin". That sharpens the choice: raising coverage means getting the single admitted leg to clear on more symbols, or getting a SECOND leg to clear at all, not tuning a blend that is not happening. Consistent with the earlier finding that alpha stacking was refuted (88% of passes ran only 2-3 live legs); it is now one. Deliberately NOT claiming which leg from the components snapshot — `PressureScore` and `ForecastProb` both appear on all 740 rows, but component presence is the INPUT snapshot and not evidence of admission, and conflating the two is the same error this row already documents. | Measured per UTC day from deduped `predictions` (1d): 07-28 through 08-05 ran 0.976–0.988 coverage on a 329-symbol cross-section, 08-06 fell to 0.927 (24 withheld), then 08-07 to **0.094** (298 of 329 withheld) and it has stayed there — 08-09 0.058, 08-10 0.064, 08-13 0.097. The 08-11 expansion is visible in the same table: considered goes 329 → **2947** with 2665 withheld. Verified NOT a regression from this session: the error is present on every revision deployed tonight (f54b7bf5, 20574033, 1e86da23, 0784bfb5, 4f54ea89, 82282bc0, f1f8933b, 05fcf440) and the collapse predates all of them by five days. Also verified the monitor itself is sound rather than miscounting — `Symbols` is deduped `predictions` rows, i.e. what the ensemble actually attempted, not the `symbols` table. Note the basis_epoch + RequireMeasuredLegs work that was meant to address leg starvation HAS since deployed, and coverage did not recover, so that remedy should not be assumed sufficient — every prediction written today carries `basis=static`, i.e. not one is running on measured legs. Diagnosis evidence for the amendment above: the 1870 delisted symbols delisted as far back as 2020-10-22 across 986 distinct dates with ZERO bars in the last 5 days, yet the newest prediction for any of them is 18:07 today while listed symbols were predicted 6 minutes ago — the backfill flipped them mid-day and the `active=1` filter took effect immediately. This is the second near-miss of the session (see F-4): both looked like live defects until recency was checked, and the correct question in each was "is this still happening, or did it already stop?" | **open** |
| **A30** | quant | HIGH | ROOT CAUSE of the `basis=static` half of A29: a ROW cap starves a DAY gate, so the adaptive layer can NEVER un-gate. `adaptiveweights.go:19` sets `adaptiveMaxRows = 20000` per horizon and `Run` pools two horizons, giving exactly the 40000 rows every cell reports. `adaptive.Compute` then gates any cell spanning fewer than 20 DISTINCT trading days — correctly, on this repo's own day-clustering doctrine ("same-day rows share one market move"). But 40000 rows now buys only 8 days, so all five cells (`all`, `uptrend`, `downtrend`, `range`, `squeeze`) come back `gated=true` with no `weights` map, `adaptive.Pick` returns nil for every symbol, and the ENTIRE fleet blends on the static equal prior. The failure is invisible because the learner reports something TRUE — "only 8 distinct trading day(s), below the 20-day floor" — that implies the wrong remedy: it reads as "not enough history yet" when the history already exists. | Measured, not inferred. `prediction_outcomes` holds 642,431 rows across **41 distinct days**; the floor is 20; the learner sees **8**. So the constraint is not data availability — there is more than twice the required history — it is the cap upstream of the gate. Confirmed the cap is binding and not coincidental: cell `n` is exactly 40000 = 2 × `adaptiveMaxRows`, and per-leg spans degrade the same way (`pressure` 8 days at 40000 rows, `alphax` 6, `sentiment` 6, `meanrev` 4). Confirmed the consequence live: EVERY prediction written today carries `basis=static`, and `Pick` (adaptive.go:466-476) returns nil only when the `all` cell has no weights, which is exactly the observed state. Why it went unseen: at design time the labeled set was "158,204 rows over 13,058 symbol-days over 23 DISTINCT" days (adaptive.go:65) — above the floor — and daily volume has since grown to ~15,700 rows/day, so an unchanged constant silently crossed from sufficient to permanently blocking. Same shape as the `LIMIT 120000` cap this session found binding at 198,002 rows. FIX (not applied — needs quant sign-off and the router was 429): make the read day-aware, taking the most recent N trading DAYS rather than N rows, so the input to a day-counting gate is measured in the same unit as the gate. Raising the constant alone would only postpone the recurrence. WHAT DID SHIP (commit 1cfa982, deployed and verified) is the visibility half, which changes no weight, probability or gate decision: the worker now records when a per-horizon read comes back full and appends a `ROW CAP BINDING` clause in the ONE state where the floor message misleads — truncated read AND zero cells learned AND days below the floor. Every other combination is left untouched, because a short read means the days are real, a learned cell means the gate is passing, and a block at or above the floor is a different gate that must not be misattributed to the cap. Proven live as a before/after on the same worker one binary apart: `rev=05fcf440` 23:09 ended at "...0 cell(s) yielded learned weights (floors: 30 rows AND 20 days...)", `rev=1cfa982a` 23:36 adds "ROW CAP BINDING — the per-horizon read returned its full 20000-row limit, so 8 day(s) is what the cap ALLOWED, not what exists". Five cases pinned via a pure helper so the clause is tested without seeding 20,000 rows. DECISION SUPPORT, measured read-only against the live DB so the sign-off is informed rather than blind — the fix WOULD work. Simulating `LabeledFeatures`' own query (`features` JOIN `prediction_outcomes`, newest-first, row-limited): **1d** has 197,625 rows over **39 distinct days** but the 20000-row cap spans only **7**; a day-aware 20-day read costs 78,376 rows and clears the floor. **1w** has 163,831 rows over **33 days**, the cap spans **3**, and a 20-day read costs 80,339 rows and clears. So the price of the fix is roughly 4x the rows read (40k pooled today → ~158k) and the benefit is that the day floor becomes reachable at all. Note the cap is even tighter than the pooled cell suggested: 7 and 3 days per horizon, not 8. HONEST RESIDUAL: clearing the DAY floor is necessary, not sufficient — cells must still pass `MinCellSamples` and the panel test, and per-cell day coverage is not the pooled figure (a cell only accrues days in which its regime occurred), so some cells may stay gated. What is settled is that the current cap makes the floor unreachable regardless of history, and a day-aware read removes that specific blocker. | **open** |
