# SignalDeck ops audit -- 2026-09-01

Scoped to ops health, correctness and honesty under the pre-registration seq 87 freeze: no signal,
threshold, guard or config changes. Revision inspected: a10892c to 38136b2 on hmm-regime-and-pbo.
Baseline gates were green going in (go vet and build, 125 Go packages, pytest tools 409 passed) and
every finding below sat behind them. Two facts reframed the pass. The running daemon 7a43a64 was 28
commits behind HEAD with an EMPTY daemon and web source diff, so no deploy was pending. And the host
has been powered off through every US session since 2026-08-24 (System log: shutdown 00:17-08:27 and
boot 16:18-19:14 daily; on 09-01 an unexpected power-off at 10:14, boot 17:54, reboot 18:07), so the
daytime tasks run only as evening catch-ups, and a boot day's task result codes are mostly the reboot.

**Status: NOT COMPLETE.** Twelve findings fixed and verified, one refuted, one accepted-risk, three open:
two need Nicholas (elevation at A2, an S3 target at A17) and one is a trade-off only he can make
(A12, where filling the gaps opposes the retention cut). A13 and A14 were found after the deploy and
were fixed the next day. A16 and A17 were A1b and A5b before the register showed it cannot parse
letter-suffixed ids.

## Findings

| id | area | severity | impact | evidence (measured) | status |
|---|---|---|---|---|---|
| **A1** | daemon/internal/llm/llm.go and daemon/.env | high | The vendor retired the default LLM model (third retirement in six weeks); sentiment-tagger and ai-analyst failed HTTP 410 on every pass from 2026-09-01 17:56, and the .env pin of the same dead id would have overridden any new default | worker_runs: tagger errors at 17:54, 18:08, 18:18, 18:28 and ai-analyst at 18:00, 18:13, all llm provider error HTTP 410. Real-completion probes: nvidia/nemotron-3-nano-30b-a3b 410 (end of life 2026-09-01T09:00Z), the catalogue lookalike nemotron-nano-3-30b-a3b 404, every meta/llama id 410, llama-3.1-nemotron-51b/70b 404. With the tagger's own charter and 120-token budget, nvidia/nemotron-3-nano-omni-30b-a3b-reasoning returned the strict JSON on 3 of 3 headlines in 2.2-5.7s (finish=stop); nemotron-3-super-120b-a12b answered in 1.9s, so its earlier 503s were overload not retirement. Defaults moved and the .env pin removed; TestTieredDefaults pins the constants; commit 2da814e; deployed as commit 38136b2 at 23:49 PT (94 of 94 worker rows stamped 38136b2a, deployment_drift 6 of 6 ok); 70 LLM calls since the restart: 0 HTTP 410, 68 ok, 2 ResourceExhausted 503s from the provider shared worker pool (16 of 16); ai-watcher ok with 7 flags on the new model; each tagger pass tags dozens of headlines and then ends on one 503, see A13 | fixed |
| **A16** | daemon/internal/aiagents/sentiment/sentiment.go and daemon/internal/pipeline/data.go | low | The tagger reported ok tagged 0 headlines when its FIRST call of a pass hit the daily LLM cap, so a starving tagger read as healthy | 19 such passes on 2026-09-01; signaldeckd.log 10:14:13 reserved=false spendToday=2128 against cap 2000. RunOnce now returns ErrCapReached with the count and the worker reports tagged N headlines; daily LLM call cap reached; TestRunOnceStopsOnCap pins the contract; commit 2da814e | fixed |
| **A2** | Task Scheduler: SignalDeck Anchor-Publish, SignalDeck Revalidation, SignalDeck Daemon Keepalive | high | Anchor-Publish and Revalidation were re-registered UNELEVATED on 08-31, so they carry LogonType Interactive, StartWhenAvailable False and DisallowStartIfOnBatteries True; the 14:35 Anchor trigger sits inside the host's daily off-window, so it never fires and the public anchor chain goes stale at 48h | Get-ScheduledTask read-back 2026-09-01: Interactive / False / True on both; Keepalive is S4U but DisallowStartIfOnBatteries True; Task Scheduler Event 153 missed its schedule at 18:03:22 and LastTaskResult 0x800710E0; logs/anchor-publish.log has no automatic run since 08-12, only the manual 08-31 19:37 push. ops/fix-task-settings-20260901.ps1 (elevated, transcripted, reads the tasks back and prints FIX-TASK-SETTINGS OK) is written and committed in 2da814e but NOT run: it needs Nicholas's UAC click, and the one supervised anchor run after it is approved but pending. RESOLVED 2026-09-09 (signaldeckfix re-audit): Get-ScheduledTask read-back shows Anchor-Publish, Revalidation and Daemon Keepalive all S4U, StartWhenAvailable True, DisallowStartIfOnBatteries False; Anchor-Publish last ran 2026-09-08 19:30 with result 0x0 and is scheduled 2026-09-09 19:30 | fixed |
| **A3** | data/signaldeck.db storage budget | high | The database was at 5729 of 6144 MB and growing about 164 MB a day (freelist_count 0, so growth is rows: bars 1332 MB, features 1261, scores 948, research_weeks 605), breaching the budget around 2026-09-04 | Nicholas chose to cut hot 1-minute bar retention from 60 to 30 days rather than raise the budget: SIGNALDECK_1M_RETENTION_D=30 in daemon/.env, effective on the 09-01 deploy; 1m rows are archived to gzip-CSV before prune and compacted to 1h, so nothing leaves cold storage. Roughly 500 MB comes back at about 164 MB a day of growth, so the budget alarm returns around 2026-09-07 unless the budget is raised or more retention is cut; stated to him at decision time. The first downsampler run after the deploy moved the oldest 1m bar from 2026-07-04 to 2026-07-06 (13.83M to 13.63M rows) before hitting its 15-minute deadline | accepted-risk |
| **A4** | ops/signaldeck-refresh.sh line 300 and data/backups | medium | A flat 12288 MB backups budget contradicted the backup script's own derive_budget_mb (2xDB plus 6x25 percent of DB, 20052 MB at 5729), paging STORAGE OVER BUDGET nightly on a folder the rotation kept healthy; an ad-hoc 5.2 GB pre_repair file sat outside rotation; the partial 08-31 gz and raw 09-01 file were the 18:07 reboot mid-compression and self-heal via gzip -f | Default raised to 21504 with a comment naming derive_budget_mb, commit 2da814e; pre_repair_added_at_2026-08-29.db deleted with approval; data/backups went from 21066 MB to 16231 MB; ops/test-backup-budget.sh PASS | fixed |
| **A5** | ops/signaldeck-backup-offline.sh | high | No off-machine copy for 21 days (last 2026-08-11). The script read SIGNALDECK_OFFSITE_S3 and SIGNALDECK_OFFSITE_DIR only from the scheduled task's process environment and never sourced daemon/.env, so the documented .env template could never take effect: a target set only there reached nobody | New ops/lib-offsite-env.sh sources both names from daemon/.env when the environment lacks them (environment wins, values never logged); selftest 4 cases PASS under the production Git Bash; wired at line 21 of the backup script; ops/test-offsite-s3.sh PASS; commit 2da814e. Against the real .env today both names read unset because no target exists yet, see A17 | fixed |
| **A17** | offsite destination | high | There is still no off-machine copy: this machine has one volume, OneDrive syncs nothing, and the aws CLI has no credentials | Nicholas chose S3 and will run aws configure and name a bucket himself; after that the single .env line SIGNALDECK_OFFSITE_S3 and an S3 lifecycle rule (daily uploads of a 5.8 GB file cost real money without expiry) complete it; verification is the next Market-Close log line offsite OK with the verified byte count and meta backup_last_offsite equal to that day. RESOLVED 2026-09-07 (repair ledger R25, Nicholas chose a private GitHub release over S3): ops/lib-offsite-gh.sh uploads the gzipped nightly backup as a release asset on SIGNALDECK_OFFSITE_GH_REPO; first upload signaldeck-20260907-131007.db.gz = 898,352,731 bytes, GitHub reported 898,352,731, newest 7 kept | fixed |
| **A6** | README.md collection claim | medium | README said SignalDeck continuously records live market data while the host has been off through every US session since 08-24 and minute bars for missed sessions are not backfilled | Measured 2026-09-01: SPY 1-minute rows per UTC day since 08-24 were 123, 4, 6 and 231, none on the days the host was off; today's capture stopped at 17:19Z (10:19 PT) and the 17:15-00:54Z gap holds 5 rows. README now states the collection window and the backfill fact; tools/verify_dod.py DOC_HEADERS PASS inside pytest 409 passed; commit 2da814e | fixed |
| **A7** | ops/market-close.sh forward-test STALE line | low | The alert named a session that was never graded (newest recorded session 2026-08-23 is 8d old, produced by COALESCE on an empty forward_test_daily); firing on the empty table is deliberate, the sentence was false | New ops/lib-forward-test.sh; live wording is now STALE: no session graded yet, forward_test_daily is empty 10d after registration 2026-08-23; ops/test-forward-test-stale.sh 4 cases PASS; commit 2da814e | fixed |
| **A8** | README.md line 278 | low | Execution stays in stock-trader contradicted pre-registration seq 87 (no execution layer is built for the book) and the existing, deliberately unwired execution-layer repository | Replaced with what seq 87 records; tools/docs_gate.py exit 0; commit 2da814e | fixed |
| **A9** | drafts/patches and bin/ | low | Six patches from 08-06 all fail git apply --check with their topics already merged; 19 rollback, pre-deploy and .bak binaries (486 MB) all older than the running binary | git rm -r drafts/patches (10 files, 2509 deletions) and the 19 binaries deleted with approval, keeping signaldeckd.exe.prev and sdmaint.exe.prev as the rollback pair; commit 2da814e | fixed |
| **A10** | ops/test-offsite-is-offsite.sh | medium | The test has been red since commit 8f3134b on 08-25: its structural awk check demanded that every backup_last_offsite mention follow the same_volume branch, so the S3 branch's write (guarded by a verified upload) and a comment above it read as UNGUARDED; nothing runs the ops tests on a cadence, so a red test went unnoticed for a week | Comments are now skipped and either guard counts; mutation: an unguarded write inserted before both guards prints UNGUARDED; the ops test loop went 10 of 11 to 11 of 11 PASS; commit 8be2bd3 | fixed |
| **A11** | Task Scheduler last results for Daily-Refresh, Market-Close, Web on 09-01 | low | Daily-Refresh 0x1, Market-Close 0x41306 and Web 0x800710E0 looked like three task defects | All three are the 18:07 reboot: refresh's catch-up collided with Market-Close's catch-up launched the same second (Event 114 twice at 18:03:22) and then hit a disk I/O error during shutdown; Market-Close was terminated by the reboot (Event 111 at 18:07:20, 4 minutes in, PT6H limit untouched); Web logs instance already running every 5 minutes from its keepalive trigger while Next.js answers on 8323. Normal days succeed (sweep done 08-26 to 08-31). If logs/refresh.log lacks a sweep done dated 09-02 the refresh reactivation step gets a busy-timeout retry. SAME CLASS, 2026-09-02: the daemon looked like it had crashed at 19:23 (workers orphaned, swept at boot). Task Scheduler Event 111 shows it was TERMINATED and the Keepalive relaunched it 68 seconds later; the daemon task is S4U with ExecutionTimeLimit PT0S and State=Running, and its LastTaskResult 0x800710E0 is the benign already-running refusal produced by MultipleInstances=IgnoreNew when the Keepalive starts an already-live task. Supervision worked; the alarming code is the known-benign one | refuted |
| **A15** | daemon/internal/api collapsegate_test.go and accuracy.go | medium | TestGate_ApiRefusalMatchesTheDocumentGate froze its fixture at 2026-08-20 while the accuracy handler judged the graded window against time.Now, so the test rotted a week after it was written and blocked the deploy gate; a cached go test ok had hidden it | Red at a10892c in a throwaway worktree; deploy attempt 1 REFUSED on it. Deps.Now (nil means the wall clock) is now used by the accuracy handler and both track-record gate sites and the test sets it; the api package passes uncached; commit 38136b2; no production behaviour changed | fixed |
| **A13** | daemon/internal/pipeline/data.go SentimentTagger.Run | low | When a pass ends on a provider error after tagging dozens of headlines, the worker returns the error alone, so worker_runs shows error with no count; both post-deploy passes tagged for ten minutes and then reported only the final 503 | Since the 23:49 restart: 70 calls, 68 ok, 2 ResourceExhausted 503 (16 of 16 workers), one per pass; RateNews persists each tag so progress is real but invisible in the run detail. FIXED 2026-09-02 in commit 84d3219 and deployed: SentimentTagger.Run wraps a failed pass as "tagged N headlines, then <error>" when N > 0. Verified live in worker_runs at 18:49 and 19:25 — "tagged 17 headlines, then llm: provider error: ResourceExhausted" and "tagged 39 headlines, then ..." where the same pass previously recorded a bare provider error | fixed |
| **A14** | daemon/internal/aiagents/analyst/analyst.go and internal/llm attemptTimeout | medium | The hourly ai-analyst timed out on its first post-deploy run: it asks the default tier for an 800-token brief and the reasoning model spends the 75-second per-attempt budget thinking, while the watcher with its short answers succeeded on the same model | worker_runs 23:49:50 error llm: request: Post ... context deadline exceeded; llm.go defaultTimeout 75s versus deepTimeout 100s; analyst.go line 266 calls client.Complete on the default tier. CONFIRMED RECURRING before acting: 6 of the 8 hourly runs after the deploy timed out. Root cause measured with the analyst's own prompt — thinking ON took 19.3s and 1058 completion tokens behind 3.5k chars of hidden reasoning; thinking OFF took 1.9s and 81 tokens and produced the same brief. FIXED in commit 84d3219 (deployed): llm.templateKwargs sends enable_thinking false to every tier except the deep one, gated on the nemotron family. Verified live: of 20 "context deadline exceeded" lines after the fix, 13 are worker run deadlines and 0 are LLM requests; ResourceExhausted refusals fell from 40 in a comparable window to 2. TestTemplateKwargsDisablesThinkingOffTheDeepTier pins both directions, mutation-verified | fixed |
| **A12** | 1-minute bar backfill | medium | Minute bars for sessions the host sleeps through are not backfilled although a backfill reconciler exists, so intraday history has holes on every off day | ROOT CAUSE FOUND 2026-09-02: BackfillReconciler re-enqueues a symbol only when its TOTAL 1m row count is below minCoverage=100 (internal/pipeline/backfill.go), a "did history ever land" floor. A streamed name holds millions of 1m rows, so a symbol missing yesterday's session never re-enqueues; nothing anywhere looks for GAPS. The machinery to fill them exists and would work: Alpaca BackfillMinute requests the last 60 days (client.go:170) with idempotent upserts. Measured: 1935 of 2943 active stock symbols sit under the floor and the reconciler re-enqueues 11 per pass. NOT FIXED, because filling every gap ADDS 1m rows and directly opposes the 09-01 retention decision that just cut them 60d to 30d to stay inside the DB budget; that trade-off is Nicholas's | open |

## Verified, no action

- The running daemon 7a43a64 carried an empty daemon and web diff against HEAD before the deploy, and deployment_drift reported 6 of 6 ok.
- The pre-registration chain has 103 rows and verify_dod reports CHAIN_INTACT PASS; seq 97 to 103 are grading-look rows the prereg-registrar worker writes once per grader look.
- audit_register reported 106 of 106 prior findings resolved, with 08-23 A9, A10 and A12 fixed.
- /proof serves the honest refusal: gated true, winRate null, grader refusing since 2026-09-01T18:04:48 over collapsed cross-sections through 08-06.
- forward_test_daily is empty because the ensemble abstains and the book yields 1 to 4 bets a day under the registered 5-bet floor; correct behaviour under seq 87, not re-decided.
- gbm, expectancy and forecast-monitor stay degraded by design.
- The tagger burns the full 2000-call daily cap on a 356613-headline backlog; left as-is because tagging volume is sentiment-leg adjacent under the freeze.

## Deploy

Deployed via ops/signaldeck-ctl.sh deploy. The first attempt was REFUSED because
TestGate_ApiRefusalMatchesTheDocumentGate had rotted on the wall clock (A15); commit 38136b2 injects
Deps.Now and the second attempt printed deploy VERIFIED at 23:49. Provenance was read from
worker_runs, not from the script: 94 of 94 rows stamped 38136b2a since 23:49:49, 0 dirty,
deployment_drift 6 of 6 ok, and the daemon logs model=nvidia/nemotron-3-nano-omni-30b-a3b-reasoning.
The 1-minute retention cut is being applied by the downsampler in 15-minute runs that hit their own
deadline; the file shrinks only after the storage governor VACUUM.
