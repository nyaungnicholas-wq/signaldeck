# Closure record — remaining low/info findings, 2026-09-13

Branch `public-launch`. This closes the tail of `verified-remainder.json` (the
low/info findings left after the blocker and high/medium passes). 13 commits, all
pushed. Every claim below was re-derived against the tree or the live database
before it was acted on; where my own first measurement was wrong, that is recorded.

**CI status: `docs-gate` is the only failing job, and that is correct.** It is
red because the grader REFUSES publication — the graded window contains 18
collapsed cross-sections of 76 days. That refusal is the truth-stop working, is a
settled decision (leave the gate alone), and is not a defect to repair. `daemon`,
`web`, `tools`, `ledger-provenance` and `pre-publish-scan` are green.

## Closed

| id | what it was | what was done |
|---|---|---|
| 0 | hardcoded developer paths | The two LIVE sites (`accuracy-registry.sh`, `archive.go`) were already fixed and only mention the path in comments explaining the fix. The real residue: two TRACKED binaries (`daemon/signaldeckd` 21.7 MB, `daemon/compact_proof` 10.9 MB — `.gitignore` had `daemon/*.exe` but not the extensionless builds), now untracked; and 18 of 22 macOS plists, now disclosed with the real count in DEPLOY.md |
| 2 | prediction write is four transactions | Root cause NOT changed — collapsing it would reverse a deliberate availability choice, and a reconciler cannot honestly backfill a hash chain. The `/proof` copy that claimed "commits **every** prediction" was corrected, and the page metadata with it |
| 3 | `var_forecasts` dead schema | Removed from `schema.sql` (0 rows, no writer, no reader, only references were its own DDL). Existing databases keep an empty undeclared table; a DROP migration on a 5.4 GB file buys nothing |
| 4 | `score_outcomes` resolved side full-scans | Partial index `(horizon, ts) WHERE resolved_at IS NOT NULL`. Measured on a 5.31 GB copy of live: build 3.0 s, +0.10 GB, plan SCAN+TEMP B-TREE → SEARCH USING INDEX, read 0.45 s → 0.014 s |
| 5 | `/market/overview` Regime tile | Already fixed (23f194a) — the tile is a modal with coverage, not `regimes[0]` |
| 10 | signed-in user on a public page | `/login` now redirects an authenticated visitor to `/dashboard`; the palette stopped calling `/` the dashboard and gained a real `/dashboard` entry. Deliberately not in the nav: the anonymous surface must not gain a per-visitor `/api/auth/me` call |
| 14 | filing date rendered in UTC | Uses `fmtDate`, the formatter the rest of the app uses, so the page is one timezone throughout |
| 15 | five legacy URLs chained two 307s | Repointed at their real destinations. Verified against a production build on a scratch port: 2 hops → 1 hop, same final URL for all five |
| 34 | DR drill never executed | Disclosed in DEPLOY.md, including that the runbook is macOS/launchd-shaped and could not run against this Windows host even with a second machine. Executing it needs hardware this pass does not have |
| 39 | dirty tree refuses both deploy paths | Clears: `sd_dirty_excluding_generated` now returns 0 lines |
| 52 | `eslint-config-next` trailed `next` | Bumped 16.2.10 → 16.3.4. It immediately found a rule that had never run (see below) |
| 57 | manual fills stamped at a synthetic ts | The trade now records the bar it filled against; the cursor still steps. Mutation-proven: the old code stamps +1 s and the book reports itself unverified |
| 58 | no idempotency key on paper orders | Documented rather than built. Recorded which defence is real (OrderForm disables on `busy` — verified) and which is not (the cursor 409 cannot fire, because the handler steps past its own bar on purpose) |
| 59 | exit fills had no lower bound | Clamped forward to the first unconsumed bar. Mutation-proven |
| 60 | manual ADV included the forming daily bar | Mirrors the flagship `advUSD` (skips unsettled and zero bars, divides by bars actually summed). The volatility half is not fixable by a filter and is now disclosed instead of claimed |
| 61 | capped-fill notice computed and discarded | `Fill.WithReason` keeps both the caller's rationale and the fill's own notice. Confirmed end to end in a real trade log |
| 63 | LONG/FLAT could be inverted | Read as a pair; an overlapping pair is refused whole via `envcfg`. Mutation-proven |
| 85/99 | params silently coerced | `windowParam` clamps instead of collapsing to the default; an unrecognised `?horizon=` is now refused with 400 instead of being answered as 1d with nothing in the payload disclosing it |
| 86/100 | routes with no consumer | Re-derived: **23 of 162**, not 17. Seven are ANONYMOUS and deliberately so (the machine-readable half of the published record) — now documented where the decision lives and added to the empty-database contract test |
| 91 | `forecastmon` "Verified 2026-08-11" comment | Expired and corrected. 2,453 of 59,629 `n_used = 0` rows now carry some other `raw_prob` |
| 92 | `worker_runs` retention | Closed earlier this pass |
| 96 | `/api/ready` JSON labelled `text/plain` | `writeJSONStatus` instead of `WriteHeader`-then-`writeJSON`. Mutation-proven |
| 46 | flaky test | Closed earlier this pass; the flake itself could not be reproduced here and the commit says so |

## Deliberately NOT done

- **19 — CSP allows `script-src 'unsafe-inline'`.** The finding's own proposed fix
  is "None required for launch". `unsafe-eval` is dev-gated, the rest of the policy
  is tight, there is zero `dangerouslySetInnerHTML` in `web/src`, and the nonce
  path is already named in the code. A decision record, not a defect.
- **35 — ~5.4 GB in the retired-backup holding area.** Reclaiming it hard-deletes
  the only local copy of a retired backup, which this pass is not authorised to
  do. The disk is 54% used with 882 GB free, the cleanup script already announces
  what it is not doing and how to change it, and the audit's own preferred fix is
  an age policy gated on a verified offsite copy — an operator decision.
- **16 — `page.new.tsx` "compiled and linted but never served".** It is
  GITIGNORED: not tracked, not in any clone, never seen by CI. A local scratch
  file on one machine. Nothing in the repo to remove.
- **The four-transaction prediction write (2).** See above.

## Measurement corrections made during this pass

Recorded because each would have shipped a wrong number:

1. **Ledger gap.** An ad-hoc join gave 4,584 (0.91%). It omitted the
   `LEDGER_START` bound and counted predictions made before the ledger existed.
   The shipped ops query gives **30 of 496,987 (0.0060%)**.
2. **Route audit.** Searching for the literal registered pattern means a wildcard
   route like `/api/evidence/{id}` can never match a caller. Fixed to search the
   fixed prefix before drawing any conclusion.
3. **`/api/research-loop` "healthy":true.** Read as fabricated confidence by a
   blunt substring check; it is correct code. The contract-test rule was made
   sharper (a confident token must be paired with a named disclosure, and the
   disclosure is asserted too) rather than weakened.
4. **`gofmt -l` on `internal/api`.** Lists most files because they are CRLF on
   disk — pre-existing, not a regression from any change here.
5. **Two mutation proofs initially passed against broken code:** one because Go
   test caching returned `ok (cached)` for mutated source (`-count=1` required),
   one because `httptest.ResponseRecorder.Header()` returns the live map rather
   than the snapshot a client receives (`Result().Header` required).
