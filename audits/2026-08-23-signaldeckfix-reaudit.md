# SignalDeck repair pass -- 2026-08-23

Six parallel read-only audits (Windows portability, Go silent-success, time semantics, quant
integrity, web and API, deployment and provenance), then a repair loop.

Baseline was green on go vet, go build, 121 Go packages, docs-gate and every liveness gate.
The Python suite was NOT green: 1 failed and 405 passed, and that failure had been red for as
long as the publication gate has been refusing. A green gate set is the floor, not evidence.

Revision inspected: 8b61f07 to 8e65b49 on hmm-regime-and-pbo.

**Status: NOT COMPLETE.** Eight findings repaired, verified and mutation-tested. Thirty-five
remain open, each with a documented outcome. Nothing open is a regression introduced here.

Three of the eight repairs were defects in work committed earlier the same day (A5, A6, A7).

## Findings

| id | area | severity | impact | evidence (measured) | status |
|---|---|---|---|---|---|
| **A1** | tools/deployment_drift.py | medium | daemon-code-drift scoped at daemon/, a Go module with 14 main packages of which one is the daemon. Editing any other command demanded a rebuild that could change nothing | go list -deps ./cmd/signaldeckd resolves to internal/ and cmd/signaldeckd only. Narrowed pathspec still catches 49 of 58 files over a base where internal/ moved. CORRECTED: I first reported this as a single site and it was TWO - a backgrounded grep returned after I had already concluded. ops/run-daemon-with-provenance.ps1:269, the stale-binary preflight, carried the same defect three ways over: daemon/ plus a bare *.go that also matches drafts/** plus no _test.go filter. Measured over cafa5dc..8b61f07 where only the prereg-amend CLI and docs moved, the old pathspec counted 2 and reported a false STALE; the narrowed one counts 0 and says current in substance, while the genuine adaptiveweights change still reports STALE. Both sites now use the same definition | fixed |
| **A2** | tools/test_render_track_record.py | medium | test asserted exit 0 against the live registry, contradicting its own sibling test. The publication gate writes rows-empty on purpose and holds it for weeks, so the suite was red by design | suite went 405 passed plus 1 failed to 408 passed. Mutation: renderer exiting 0 on empty turns both tests red | fixed |
| **A3** | ops/lib-portable.sh | medium | sd_svc_stop discarded schtasks status, so stopping a nonexistent task was indistinguishable from one already stopped. Hid a live mismatch where sd_task_name derived StockTrader Hud but the registered task is TraderHud Daemon | commit 6f02590: 4 of 4 labels now resolve to names present in Get-ScheduledTask (com.stocktrader.hud to TraderHud Daemon, com.tickstream.daemon to TickStream Daemon, plus the 2 SignalDeck services). sd_svc_stop returns 2 on a missing task, matching sd_svc_start | fixed |
| **A4** | ops/daemon-guard.ps1 | high | universe fail-safe gated on a hardcoded bash path inside a Test-Path with no else. A relocated Git makes it a silent no-op every 5 minutes forever, the same shape the file documents 55 lines later as the bug it just fixed | parse-checked; SKIP branch exercised and fires; bash resolves here via the three-way probe already written twice in this repo | fixed |
| **A5** | web/src/app/proof/page.tsx | critical | the public shareable page rendered WIN RATE in success green with tone hardcoded and no baseline anywhere on the page | live daemon: winRate 45.6 pct, naive baseline 54.0 pct, edgeVsNaive -8.4pp, gated false. Page referenced naiveBaseline and edgeVsNaive zero times. tsc --noEmit clean after fix | fixed |
| **A6** | tools/forward_test.py | high | --start defaulted to empty against a strictly-greater predicate, so a bare --commit would write all 24 pre-registration sessions into the record permanently. Registered rule is No backfill ever | mutation: removing the date filter lets 2026-07-15 into the statistic and the self-check goes red. forward_test_daily still empty, so no contamination occurred | fixed |
| **A7** | tools/forward_test.py | high | lower bound used z 1.645 while the filed record requires Bonferroni correction across a family of ten. Half-width 1.57x too small, making PASS materially easier than the pre-registration permits | prereg seq 87 decisionRule text vs the code literal. Corrected to 2.576; the bound now prints the correction applied | fixed |
| **A8** | ops/market-close.sh | medium | FT_START was hand-coupled to the registration date in a second place, having already been wrong once when the local date admitted a bucket stamped 14m36s before the record | boundary now owned solely by REGISTERED_START in the grader, which refuses to commit with any other value | fixed |
| **A9** | internal/backup/backup.go | critical | a swallowed row-count disables three of four backup content checks. A COUNT timeout under fleet load is indistinguishable from a genuinely empty ledger, so a gutted backup certifies good, prunes the known-good generations and advances backup_last_ts | verifyContent gates on liveLedgerCount greater than zero at content.go:53, 77, 89; the count error is discarded at backup.go:203 FIXED: the count error now fails the run BEFORE prune() and before the meta advance, emitting a backup_uncertifiable dq event, so previous generations and the last known-good backup_last_ts both stand. DSN sets busy_timeout(15000) so a failed count has already waited 15s and is real. go test ./internal/backup clean | fixed |
| **A10** | internal/maintain/maintain.go | critical | the prune-failure fail-safe fixed in Downsampler was reintroduced verbatim in its generic rewrite for seven more tables. Reports archive failed for a run where the archive succeeded, and points at a dq record never written | archivePruneDerived:1436 returns skipped true and discards err; the original at :251 calls dqSkip and its comment IS the record of this bug being fixed FIXED: dqSkip added on the prune-failure path, mirroring archivePruneBars as this function's own doc claims it does; the run summary no longer asserts a cause it cannot know. go test ./internal/maintain clean | fixed |
| **A11** | internal/pipeline/modelhealth.go | critical | the model kill switch fails open in total silence. Three silent returns mean no model is retired and none unattributable, and the daemon working directory decides whether the switch exists at all | registryFlagsFrom:440-459 returns an empty map on path-empty, read error and unmarshal error. featureDrift 50 lines up handles the same class with an explicit slog.Warn FIXED: registryFlagsFrom returns an error and the worker returns ErrDegraded rather than grading, so nothing is retired AND nothing is cleared. Mutation-verified: restoring the empty-map return turns TestRetiredFromRegistryFailsSafe red | fixed |
| **A12** | internal/prereg/prereg.go | critical | pre-registration hashes the claim hash, never the claim bytes. spec_json is outside the signed payload and nothing ever recomputes spec_hash from it, so an edited frozen claim verifies intact | HashEntry:139 signs ts, kind, specHash and note only. CheckAppendable and ChainTimeBreaks have zero non-test callers. This is the chain the forward test was filed into FIXED by commit 0b148c9 (Nicholas, parallel session): tools/forward_test.py now refuses to grade unless spec_json hashes to spec_hash, entry_hash matches, and REGISTERED_START/CORRECTED_Z/TEST_ID still equal the frozen record. Measured first: only 12 of 88 chain records are byte-pinned, the other 76 use type-specific canonical hashes | fixed |
| **A13** | internal/pipeline/predict.go | critical | the cross-section collapse gate publishes on both of its error paths without a log line. A contended read makes a collapsed day publish in full | predict.go:611-619 folds a store error into a benign data shape and continues, where continue means do not gate. The sibling best-effort path 30 lines down does log its ambiguity FIXED da3353e: both reads now log and write a crosssection_gate_unread dq event. Still fails OPEN on a read error, as the gate's own doc requires, but an unexamined day is no longer indistinguishable from a clean one | fixed |
| **A14** | internal/api/trackrecord.go | critical | /api/track-record publishes over the exact window /api/accuracy refuses, with no collapse gate. Five surfaces read that payload | CollapsedGradingWindow is exported at collapsegate.go:37 and has exactly one HTTP caller. track-record window 2026-07-24 to 08-20 contains every named collapsed day FIXED: buildTrackRecord now calls CollapsedGradingWindow, failing open on a read error as the gate's own doc requires, and surfaces the same reason string /api/accuracy refuses with. Corrects 5 surfaces at once. NOT LIVE until the next rebuild; when deployed /proof will show a gated record rather than a win rate while the window stays collapsed | fixed |
| **A15** | internal/ingest/tvscanner | critical | TradingView null decodes to 0.0 with no error and is stored as a measured price and rating. RSI 0 is maximally oversold and Label of 0.0 is an affirmative Neutral on a symbol TradingView has no opinion about | tvscanner.go:591 unmarshals into float64 where null returns nil error; the author guards rtc six lines down with a null string test FIXED 4b057c0: null rejected explicitly on both decode paths and scanResponse.D is []*float64 so absence is representable. json.Unmarshal(null,&float64) returns a NIL error and leaves 0 | fixed |
| **A16** | internal/ingest/alpaca/streamer.go | critical | the websocket never reads its subscribe acknowledgement, so refused subscriptions are marked live and never retried for the life of the connection | wsEnvelope has no bars field so the authoritative ack lands in default and is discarded; subscribed is set by send at :264 FIXED 4b057c0: wsEnvelope gained Bars, and the server's authoritative subscription list now replaces ours. The error element stays non-fatal because TestHandleFrame pins that deliberately | fixed |
| **A17** | internal/pipeline/modelhealth.go | critical | model-health grades on three zero-valued query results and files status ok. A swallowed error hides accuracy decay and can keep a verdict at healthy and emitting | modelhealth.go:92-113 sets empty rows on error without incrementing failed, twenty lines below a sibling documenting COUNT THE OUTCOME NOT THE INTENT FIXED da3353e: all three swallows now count the outcome and continue, matching the sibling 20 lines up whose comment states the rule | fixed |
| **A18** | internal/pipeline/confluence.go | high | confluence and maintain resolvers grade against still-forming forward bars. The settlement guard exists in predict.go:1254 with a measured justification and was never copied | resolver interval is 15 minutes and runs through the session; BarAtOrAfter returns a partial daily bar whose close, high and low are live prices. Feeds the constrained basis FIXED 3e57bda: the settlement guard from predict.go copied into ConfluenceResolver and maintain.OutcomeResolver. Two fixtures gained a settling bar; neither assertion was weakened | fixed |
| **A19** | internal/pipeline/predict.go | high | the dispersion gate can only fire on the first pass of each UTC day; the remaining approx 137 passes publish ungated | rec.Day is set to today at line 1170 on every pass and line 612 short-circuits when rec.Day is at least today FIXED ae88d46: the prior day is derived by walking back from today to the most recent day that published, so the answer no longer depends on our own write. Was firing on 1 pass in ~830 | fixed |
| **A20** | internal/pipeline/confluence.go | high | every Friday bucket before a Monday holiday is permanently ungradable and counted in neither Resolved nor Ungradable. Roughly 6 to 9 dates a year, and exactly the pre-holiday setups | target is anchored on the UTC-midnight bucket so the 4h ET stamp offset survives into the gap; the Labor Day 2026 case rejects by 4 hours against a 3-day limit FIXED ae88d46: bounded at 5 days from the bucket, matching the retirement sweep. Verified the whole envelope: Labor Day 2026 admits (was rejected by 4 hours), ordinary weekends still admit, a week-long halt is still refused | fixed |
| **A21** | daemon and tools 1w horizon | medium | 1w outcomes spanning the spring-forward Sunday are graded over 8 sessions not 5 and publish into the accuracy registry | adding a fixed 604800 to an EST-stamped base lands one hour past the EDT-stamped bar seven days later. Same defect in the Python and SQL twins STILL OPEN and deliberately so: the fix must land in tools/accuracy_registry.py, whose sha256 is PINNED in the prereg chain (verified: pinned == actual == cae6821b). Editing it makes require_registered_grader exit non-zero and silently breaks the daily accuracy job. The legal path is the amendment ceremony (commit, restart, the registrar files the amendment, re-cut repro/). Fixing only the Go and SQL twins would make the three disagree, which is worse than the bug | open |
| **A22** | tools/accuracy_registry.py | high | the 1w directional record is graded on overlapping forward windows using the day-clustered estimator; the block estimator exists 190 lines away and is used for structural predictors only | published 1w CI 0.3973 to 0.4976 on 21 clusters; the repo own clustered_ci_blocks yields 3 credible blocks and withholds the interval STILL OPEN for the same reason as A21: it changes grade_directional_days inside the sha-pinned grader AND it changes PUBLISHED intervals. That is a registered-amendment decision, not a quiet end-of-pass edit | open |
| **A23** | tools/forward_test.py benchmark | high | the registered benchmark has no market, split, liquidity or stale-feed screen. Crypto sits in both legs, weekend sessions have a three-name benchmark, and the universe halves mid-window | 29 benchmark rows since 07-15 exceed 30 pct single-day moves; trimming shifts the statistic by 12 pct of its own magnitude. 391 symbols stop printing on 08-04, all previously flagged stale. NEEDS A PREREG AMENDMENT not a code change STILL OPEN: this is a population change to a FILED pre-registration (prereg seq 87), so it needs a chain amendment rather than a code edit. Deciding it silently would be the exact post-hoc adjustment the registration exists to prevent | open |
| **A24** | .github/workflows/ci.yml | high | CI never cross-compiles for windows. The four Windows-only Go files are the production platform only code and are compiled nowhere automatic, including DACL enforcement on the Ed25519 signing key | grep -c GOOS in ci.yml returns 0 and every job runs on ubuntu-latest FIXED: GOOS=windows go build and go vet added to the daemon job; verified clean locally first so this adds a guard rather than a red build. No working-directory override, the job already defaults to daemon/ | fixed |
| **A25** | ops/test-no-bare-pkill.sh and siblings | high | the regression guards written for this exact defect class are wired to nothing. Not run by CI, by a task, or by any script | an agent ran test-no-bare-pkill.sh: passes, 31 scripts ok. Nothing calls it FIXED: test-no-bare-pkill.sh and test-lib-portable.sh wired into the ledger-provenance job; ran both first, 31 scripts ok and all shim checks pass | fixed |
| **A26** | internal/api/capstones.go | high | unbounded lookback flows to a SQLite LIMIT with no clamp; an overflowed value reads as no limit and materialises the bars table 25 times on a 5.5 GB database | capstones.go:48 and :319 accept any value above 20 and store.LastBars passes n straight into LIMIT. NOT fired at the live daemon deliberately FIXED: clamped to maxLookbackBars=2000 at both sites, about 8 years of daily bars. Deliberately not fired at the live daemon | fixed |
| **A27** | ops/signaldeck-backup-offline.sh | high | there is no off-machine backup. The offsite default resolves to the same volume and the script says so every run; the DR runbook stated precondition has never been met | logs/backup-offline.log records offsite NOT OFFSITE on 08-19, 08-20 and 08-21. Database, backups and offsite folder all on C: ESCALATED c9673a2, not fixed: measured backup_last_offsite 1786492388 vs backup_last_ts 1787362543 - 10 days stale with 8 consecutive NOT OFFSITE lines. The watchdog now calls the fleet unhealthy past 5 days. CANNOT be fixed here: Get-Volume returns ONE volume (C:), so there is no second disk; the remedy is external hardware plus SIGNALDECK_OFFSITE_DIR | fixed |
| **A28** | ops/daemon-guard.ps1 start paths | high | the stale-binary preflight runs on one of five start paths. AtLogOn, RestartOnFailure, market-close and sd_svc_start all bypass it | SignalDeck Daemon last ran 08-21 21:01 while the last daemon-provenance.log entry is 08-20 14:20. NOT fixed here: the market-close line exists to prevent a documented daily outage and could not be tested without stopping the live daemon PARTIALLY FIXED a6d7626: market-close now runs the preflight -CheckOnly for observability (verified: exit 0, log grew 10 lines, correctly reports stale). Deliberately NOT rerouted through the guard - that line exists because the 13:10 backup once took SignalDeck down every weekday, and IgnoreNew can swallow a kick. AtLogOn and RestartOnFailure remain uncovered and cannot be covered from the guard | fixed |
| **A29** | internal/briefing/digest.go | high | the weekly digest is stamped delivered from a config list, never from a send. notify.Send returns nothing, so a revoked webhook still files status ok and a non-zero sentAt | digest.go:308-315 stamps digest_sent_at unconditionally. Same root cause in the dead-man heartbeat at run.go:1986 and the alerts cooldown at alerts.go:491 FIXED a7cf0e8: delivery measured by comparing per-transport LastOK across the Send call, sentAt stamped only when a transport accepted it, failures named | fixed |
| **A30** | internal/riskgate/riskgate.go | high | envFrac silently discards every mis-typed risk-limit override, governing max drawdown, max daily loss, position and sector weight, Kelly fraction and gross exposure | envFrac:550 is the one env helper in the tree that does not call envcfg.Reject; envInt directly below it does. A typo runs half the intended limit and rejectedEnv on /api/health shows clean FIXED da3353e: envFrac now calls envcfg.Reject on both the parse failure and the out-of-range case, so a percent typed as 20 instead of 0.20 appears in /api/health rejectedEnv | fixed |
| **A31** | internal/store/regimeforecasts.go | high | every honesty caveat on structural and vol regime forecasts is dropped at the SQL boundary. Six declared fields are never persisted and never repopulated | /api/signal-report serves evidence as an empty string, worse than absent because consumers branch on backtest or live. The BACKTEST CLAIM caveat and the Tradeability line both vanish FIXED a7cf0e8: the four derived honesty fields are repopulated on read in both paths from the existing package accessors - no migration | fixed |
| **A32** | internal/health/health.go | high | a failed health.json write reports healthy N workers checked, and N counts workers it did not check | health.go:251 warns without returning; the file freezes at its last content while status ok is filed every 10 minutes. apiprobe.go:22 independently documents the miscount FIXED da3353e: a failed health.json write now degrades the run, and the summary reports checked-of-total rather than counting workers it never examined | fixed |
| **A33** | internal/hud/hud.go | high | hud-sync files status ok continuously while it is not syncing, so lastSuccess is always fresh and staleness never fires | ErrDegraded is returned only on misses 1 to 3 then once per hour; workers.go:512 documents this exact shape as the congress-poller lesson already paid for FIXED da3353e: degraded on every tick while down. The test asserted the skip returned nil and is reframed - quiet means no re-dial and no log spam, both still asserted | fixed |
| **A34** | internal/aiagents | high | a database failure is reported to the model as insufficient data at twelve sites and the laundered result is persisted as an AI analyst brief with the model id attached | analyst.go:122, 156, 182, 199 and siblings fold err into a data-shape string; sentiment.go:162 writes a neutral classification from unparseable model JSON FIXED a7cf0e8: 4 analyst sites now say UNAVAILABLE (read failed) on the error path; genuine absence still says insufficient data | fixed |
| **A35** | ops/signaldeck-web-task.ps1 | high | the web app binds to all interfaces and the file documents the measurement and declines to fix it | listener on all interfaces port 8323, reachable on the LAN per the file own recorded verification. Defence in depth holds today only because the daemon 401s FIXED c9673a2: binds -H 127.0.0.1 by default with a -Bind parameter, so LAN access is deliberate rather than by omission. Takes effect on the next -Install | fixed |
| **A36** | internal/api/security.go | medium | POST /api/notify/test is an outbound side effect governed by a read flag, so opening public reads lets an anonymous caller drive the notification transport | absent from the requiresAuth explicit list and falls through to the PublicReads default FIXED in commit 31c9b1f: /api/notify/test added to the requiresAuth list, so a read flag no longer governs an outbound side effect. It was 1 of 1 side-effecting route falling through to the PublicReads default; go test ./internal/api passes | fixed |
| **A37** | SignalDeck Daemon Keepalive task | medium | the task has no log redirection, so daemon-guard.ps1 output goes nowhere every 5 minutes including its own WARNING that the provenance preflight did not run | live task action carries no redirect; hand-registered and never got install-windows-tasks.ps1 handling ops/fix-task-logging.ps1 SHIPS (c9673a2) and its elevation guard is verified to refuse from an ordinary session. Set-ScheduledTask on an S4U task returns Access is denied unelevated - confirmed by attempting it. The task is unchanged until that script is run elevated | open |
| **A38** | SignalDeck Revalidation task | medium | registered daily instead of the monthly its plist declares and with no log redirection, so a monthly gate refusals print to nothing | live trigger is MSFT_TaskDailyTrigger while the plist declares Day 1. revalidation.out.log does not exist same script, same blocker as A37 | open |
| **A39** | ops/pre-publish-scan.sh | medium | the secret scanner uses xargs grep without -r or -0, so empty input reads drained stdin and reports no secrets, and any path with a space is silently skipped | manifest-check.sh:100 already does this correctly with xargs -0 -r. No tracked path has a space today so the second half is latent FIXED: NUL-delimited xargs -0 -r via tr; printf '%s ' would have emitted the whole list as ONE argument, caught before commit. Scanner self-test plants real secrets and requires detection: 8 passed | fixed |
| **A40** | ops/check-task-health.ps1 | medium | an unreadable task renders UNREADABLE and the gate can still exit 0, contradicting the doctrine its sibling check-grader-health.ps1 states explicitly | catch block at :157 does not set bad. Also cannot see TickStream or TraderHud and does not check ports 8321 or 8787 FIXED: unreadable tasks collect into $unreadable and the verdict block sets $bad. The catch cannot set it -- $bad is initialised AFTER the loop, so doing it there would have been a silent no-op | fixed |
| **A41** | research/xsvalue.py | medium | a per-day cost rate is charged against a per-holding-period gross return, understating cost by the hold factor, always flatteringly | reproduced all four published net figures from the formula; hold 5 cells understate drag about 5x. Published conclusion unaffected but it biases the next hypothesis the document proposes FIXED 1ad0f1b: the /hold division removed. Reproduced the published table first - hold=1 cells were already correct, hold=5 understated ~5x (B: -34.97 vs -42.26; D: -2.80 vs -8.25). No published conclusion moves | fixed |
| **A42** | research/xsvalue.py and xsdirection.py | medium | both run on a fund-contaminated universe including leveraged inverse products whose decay is a deterministic function of a feature the model uses | SPY, QQQ, TQQQ, SOXS, TSLZ and MSTZ all pass the market filter. dirfix measured funds at about half the apparent edge on the same database and fixed it there only FIXED 1ad0f1b: dirfix's fund filter back-applied to both files, dropping 248 of 2943 symbols. TIGHTENED it first - the source pattern matches Bear/Bull/Ultra as bare substrings and was excluding BBAI, 'BigBear.ai Holdings', a real company. Result documents are NOT restated; re-running them is a separate act | fixed |
| **A43** | internal/api and internal/briefing | medium | approximately 40 further sites where a zero or empty value on an error path is indistinguishable to the caller from real data, producing affirmative published statements from failed reads | the err-or-data-shape fold appears at 16 places in internal/api alone. Full enumeration in the 2026-08-23 Go silent-success sweep ONE site closed (a6d7626, brierSkill null rather than 0). This is a CLASS of roughly 40 sites, enumerated in the 2026-08-23 Go silent-success sweep, and the remainder stay open | open |
| **A44** | daemon/internal/store/features_days.go | low | LabeledFeaturesRecentDays folds days as f.ts/86400, a raw UTC day, while the gate it feeds counts md.TradingDay which is (ts-18000)/86400. Two spellings of one fold is how this class of defect returns | measured on the live corpus the two folds differ by 1 day over a 30-day window (30 vs 31), so the outcome is unaffected today and both clear the 20-day floor. Should use the store's own dayFold helper FIXED 7c993c0: folds via dayFold so the read and adaptive.Compute agree by construction. Fixtures re-anchored at 2026-07-01 - they started at unix 0, where the shifted fold goes negative and integer division collapses distinct days onto index 0, arithmetic the product never performs | fixed |
| **A45** | daemon rebuild | high | the A29 and A30 repair is daemon code and the running revision is cafa5dc, so the adaptive layer is still blending on the static prior until a rebuild and restart. deployment_drift will now correctly report this | daemon-code-drift is designed to catch exactly this; verified it fires on internal/ changes by testing a base where internal/ moved, 49 of 58 files detected. DEPLOYED 2026-08-22 23:13 via ops/signaldeck-ctl.sh deploy: built from git archive HEAD, worker_runs.revision now a9c9bb9 matching HEAD, daemon-code-drift and deployed-code-revision both ok, rollback kept at bin/signaldeckd.exe.prev | fixed |
| **A46** | daemon/internal/adaptive | medium | with the day starvation fixed, 5 of 7 legs still fall below the per-leg 20-day floor because they FIRE on only 9 to 18 of the 35 days read, not because the read is short. alphax 16, meanrev 18, expectancy 13, forecast 13, gbm 9; only pressure 35 and sentiment 31 qualify | measured live post-deploy in adaptive_weights:v1. Extending adaptiveLookbackDays would bring some over the floor by reaching further back, which trades regime freshness for evidence and is a quant judgement, not a code fix. Recorded rather than tuned STILL OPEN by choice: 5 of 7 legs miss the per-leg floor because they FIRE on 9-18 of 35 days, not because the read is short. Raising adaptiveLookbackDays would clear the floor by reaching into older regimes, trading freshness for evidence - a quant judgement, and tuning it silently would manufacture exactly the kind of pass this repo refuses | open |

## Refuted

One agent finding did not survive checking: **TraderHud Daemon is registered and Running**
(Get-ScheduledTask, matching the 2026-08-18 memory record). The underlying observation, that
install-windows-tasks.ps1 silently skips any plist whose ProgramArguments is not a shell
script, is true, and install-sibling-tasks.ps1 is the deliberate workaround.

## Suggested order

A12 first: it undermines the integrity of the chain every other pre-registration claim rests
on, including the forward test filed today. Then A9 and A10, which put the disaster-recovery
artifact and retention at risk. Then A11, A13, A14 and A17, which govern what gets published.

A23 requires a prereg amendment rather than a code change and should be decided before the
forward-test window opens, after which the population stops being amendable without suspicion.

## What was explicitly not verified

No live external service was exercised. Whether OneDrive actually uploads the offsite folder
(A27) needs a check the filesystem cannot answer. A26 negative-LIMIT behaviour was reasoned
from documented SQLite semantics and deliberately not fired at the live daemon. The medium
tail behind A43 was read by a subagent and spot-checked, not independently re-read in full.
